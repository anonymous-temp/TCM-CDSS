# 中医CDSS 部署运行手册（82.156.128.153）

> 本手册给出**你自己在服务器上执行**的命令。我不会代持密码/代 SSH 生产机。
> 目标：以**安全默认**（鉴权开、PHI 不裸奔、审方 fail-closed）把当前已修版本上线，先冒烟、再对外。

---

## 0. 动手前（务必先做）

1. **改掉你刚贴出的密码**：`82.156.128.153` 的 `ubuntu` 密码已出现在对话里，视为泄露，请**立即修改**，并为部署配 SSH 公钥（`ssh-copy-id`），之后用 key 登录，别再用密码。
2. 确认服务器已装 Docker + Docker Compose v2：`docker --version && docker compose version`（没有就 `sudo apt-get update && sudo apt-get install -y docker.io docker-compose-plugin`）。
3. 确认**灵犀审方引擎**在这台机上的**监听地址和端口**（下面 §3 要用）。

---

## 1. 拉代码到服务器

```bash
# 在服务器上，选一个部署目录
git clone <你的仓库地址> tcm-cdss && cd tcm-cdss
# 或已存在则： cd tcm-cdss && git pull
```
> 若没有 git 远程，可在本地 `tar czf tcm-cdss.tgz --exclude node_modules --exclude .next .` 后 `scp` 上去解压。

---

## 2. 生成密钥（一次性，勿提交）

```bash
# 三个必填密钥（compose 里是 :? 强制项，缺了直接构建失败）
echo "CDSS_API_TOKEN=$(openssl rand -hex 32)"
echo "CASE_SNAPSHOT_ENCRYPTION_KEY=$(openssl rand -base64 32)"   # A256GCM，32 字节
echo "REASONING_CONTRACT_SIGNING_KEY=$(openssl rand -hex 32)"
```
把输出记下来，填进下一步的 `.env`。

---

## 3. 生产 `.env`（放在仓库根目录，`chmod 600 .env`）

```dotenv
# —— 必填 ——
IMAGE_TAG=tcm-cdss-20260713-1                 # 不可变镜像标签，每次发版换新值
OPENAI_API_KEY=<你的 deepseek key>
CDSS_API_TOKEN=<上一步生成>
CASE_SNAPSHOT_ENCRYPTION_KEY=<上一步生成>
REASONING_CONTRACT_SIGNING_KEY=<上一步生成>

# —— 模型（compose 已有安全默认，可覆盖）——
OPENAI_BASE_URL=https://api.deepseek.com
OPENAI_MODEL=deepseek-v4-pro
PRIMARY_PRESCRIBE_MODEL=deepseek-v4-pro
PRIMARY_PRESCRIBE_REASONING_EFFORT=medium
PRIMARY_TEXT_REASONING_EFFORT=low            # 上线若要更稳的推理质量可调 medium

# —— 安全（默认即安全，别改弱）——
CDSS_REQUIRE_API_AUTH=true                    # 鉴权必须开
CDSS_SECURE_COOKIE=true                       # 需经 HTTPS 反代
CDSS_TRUST_PROXY_HEADERS=true                 # 仅在下方反代清洗并重写转发头后启用；strictReady 强制校验
APP_BIND_IP=127.0.0.1                         # 只绑本机，前面挂 TLS 反代
APP_PORT=3000
NEXT_PUBLIC_BASE_PATH=/tcm-cdss               # 子路径挂载；若独立域名根路径可留空

# —— 灵犀审方（见 §3.1 决策）——
RXAI_AUDIT_ENABLED=true
RXAI_AUDIT_API_KEY=<灵犀 API key>
RXAI_AUDIT_BASE_URL=<见 §3.1>
RXAI_AUDIT_ALLOW_INSECURE_HTTP=<见 §3.1>
RXAI_AUDIT_TENANT_ID=EH_INTERNET_HOSPITAL
RXAI_AUDIT_SYSTEM_CODE=EH_INTERNET_HOSPITAL
```

### 3.1 灵犀 transport —— 关键决策（三选一）

代码里 `getRxAuditConfig()` 只允许两类传输：**HTTPS**，或**回环 HTTP（仅 127.0.0.1 / localhost）**。对公网 IP 的明文 HTTP 会被判 `rxaudit_insecure_transport` → 审方停用 → 处方 fail-closed 到人工复核（本次已修：这种情况会锁定采纳门）。所以：

| 方案 | 配置 | 说明 |
|---|---|---|
| **A. 容器共享主机网络（推荐冒烟用）** | compose 加 `network_mode: host`；`RXAI_AUDIT_BASE_URL=http://127.0.0.1:<灵犀端口>`；`RXAI_AUDIT_ALLOW_INSECURE_HTTP=true` | 灵犀就在本机，回环最简单。注意 host 网络下端口映射失效，app 直接监听 3000。 |
| **B. 灵犀走 HTTPS** | `RXAI_AUDIT_BASE_URL=https://<灵犀域名>`；`RXAI_AUDIT_ALLOW_INSECURE_HTTP=false` | 最干净，生产首选。 |
| **C. 暂不接审方** | `RXAI_AUDIT_ENABLED=false` | 审方停用即 fail-closed：能出候选方，但**处方一律不可一键采纳、须药师人工复核**。可做开发冒烟，但严格健康检查不会放行正式发布。 |

> ⚠️ 绝不要用 `RXAI_AUDIT_BASE_URL=http://82.156.128.153`（公网明文）——会被安全传输拦掉，等同方案 C，还白白暴露风险。

---

## 4. 构建并启动

```bash
docker compose --env-file .env build
docker compose --env-file .env up -d
docker compose logs -f tcm-cdss        # 看启动日志，Ctrl-C 退出
docker compose ps                      # healthcheck 变 healthy 即就绪（约 30-60s）
```
> healthcheck 打的是 `/api/diagnosis/health?strict=1`（带 token），`strictReady=true` 才算健康。

---

## 5. TLS 反向代理（对外必须）

容器只绑 `127.0.0.1:3000`，**必须**在前面挂 HTTPS 反代（Nginx/Caddy），否则 `CDSS_SECURE_COOKIE=true` 的登录 cookie 不会下发、外部也访问不到。Caddy 最省事：

```
your-domain.com {
    handle_path /tcm-cdss/* {
        reverse_proxy 127.0.0.1:3000
    }
}
```
（`NEXT_PUBLIC_BASE_PATH=/tcm-cdss` 要和反代路径一致；用独立域名根路径则把 basePath 留空、反代到 `/`。）

---

## 6. 冒烟测试（部署后自查）

```bash
BP=/tcm-cdss                                   # 与 NEXT_PUBLIC_BASE_PATH 一致
TOK=<你的 CDSS_API_TOKEN>
# 1) 健康
curl -s -H "x-cdss-api-token: $TOK" "http://127.0.0.1:3000${BP}/api/diagnosis/health?strict=1"
# 2) 模型连通（会真调一次模型）
curl -s -H "x-cdss-api-token: $TOK" "http://127.0.0.1:3000${BP}/api/diagnosis/model-health?check=1"
# 3) 无 token 应 401（验证鉴权确实开着）
curl -s -o /dev/null -w "%{http_code}\n" "http://127.0.0.1:3000${BP}/api/diagnosis/health"
```
浏览器再走一遍：登录 → 录一个基层病历 → M01→M05 跑通 → 看处方与审方提示。
> 想跑金标准回归：`BASE_URL=http://127.0.0.1:3000${BP} CDSS_API_TOKEN=$TOK npm run regress:tcm-cdss`（需服务器上有 Node 与源码）。

---

## 7. 回滚

```bash
# 每次发版用新 IMAGE_TAG；回滚就把 .env 的 IMAGE_TAG 改回上一个可用值再 up -d
docker compose --env-file .env up -d
```

---

## 附：本次上线前已收口的安全项

- ✅ 红旗漏报 fail-open（"黑便3天，无腹痛" 类）已修（14/14 探针）。
- ✅ 灵犀 BLOCK/需人工复核 → 采纳门 fail-closed 锁定（十八反处方不可一键写回医嘱）；审方不可用亦 fail-closed。
- ✅ 体温"38度9"口语写法可解析（脓毒症红旗不再漏）。
- ✅ 生产 compose 默认安全：鉴权开、只绑回环、密钥强制、审方明文传输默认禁。
- ⏳ 仍待修的 P0（HIGH/MED，见 `docs/深度审查报告-20260713.md`）：儿科定性词剂量门（#10）、跳过 M02 不筛自杀（#6）、证据校验 fail-open（#8）、倒置血压（#20）、审方 banner 默认绿（#16）、M04 重试不改参数（#30）。这些不阻断"能安全上线冒烟"，但对外正式使用前应逐个收口。
