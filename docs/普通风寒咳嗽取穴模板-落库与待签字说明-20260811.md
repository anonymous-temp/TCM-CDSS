# 普通咳嗽·风寒袭肺证 取穴模板 —— 落库内容与待签字说明（2026-08-11）

**当前状态：`pending_clinician_review`。模板已全部落库并上线，但整条不启用，本例仍按评估态呈现。**
中医师签字后只需改数据一行、跑门禁、重新部署，**不需要改代码**。

---

## 一、为什么要新建这条模板

甲方线上实测：风寒咳嗽给出**承灵、孔最、肩中俞**，缺列缺、风池。

第一轮只调整了评估分支的排序（孔最消失、风门升首位），列缺、风池仍拿不到。继续排查后确认
根因有两层，都不是排序能解决的：

1. **适应证标签解析走岔**：「流清涕」先命中 `upper_airway`，`respiratory` 排在其后，
   于是够不到呼吸类模板的穴位池。
2. **目录里根本没有「普通风寒咳嗽」模板**：针刺呼吸类只有两条——
   湖南省 2025 **流感**专项方案（列缺、合谷、风池、太阳、外关）与**感染恢复期**方案。
   把流感专项套到普通风寒咳嗽上属于**扩大适应证**，不能由我方自行决定。

因此按 13 条证型配穴的同一流程出裁定稿送中医师。

## 二、中医师裁定（含对我方设想的一处修正）

> 「列缺应固定进入普通咳嗽主穴，风池不应对所有风寒咳嗽强制加入，
>   而应作为鼻窍、头项症状明显时的**条件加穴**。」

理由：支持风池的北京市卫健委内容属**政府健康科普**，撑得起「可选用」，
撑不起「所有普通风寒咳嗽的固定主穴」。

| 项目 | 裁定内容 | 落库状态 |
|---|---|---|
| 病种·证型 | 普通咳嗽·风寒袭肺证/风寒束肺证 | ✅ |
| 裁定结论 | 其他（建立独立受治理模板） | ✅ 模板 id `acupuncture-common-cough-wind-cold` |
| 主穴 | 肺俞、中府、列缺、太渊 | ✅ |
| 风寒加穴 | 风门、合谷 | ✅ 与主穴同列（模板本身由风寒证闸门锁定，见 §3） |
| 条件加穴 | 风池：鼻塞流清涕明显，或兼头痛、项强时加入 | ✅ 单开 `conditionalPoints` 一档 |
| 不纳入固定穴 | 孔最、承灵、肩中俞 | ✅ 且评估态排序里也已退出前 5（§4） |
| 适用范围 | 成人普通急性/亚急性咳嗽，已明确风寒袭肺或风寒束肺 | ✅ |
| 排除范围 | 流感/流行性感冒、感染恢复期、单纯鼻炎无咳嗽、风热犯肺、红旗病例 | ✅ 逐条落库 |
| 当前状态 | `pending_clinician_review`，签字前不启用患者级模板 | ✅ |

### 来源与等级（如实分档，不合并）

| 来源 | 支撑内容 | 登记等级 |
|---|---|---|
| 《咳嗽中医诊疗专家共识意见（2021）》 | 主穴 肺俞、中府、列缺、太渊；风寒袭肺加风门、合谷 | `professional_society_consensus`（专家共识，**不是**学会标准） |
| 北京市卫健委健康科普 | 外感风寒后咳嗽可选列缺、风门、合谷、**风池** | `government_primary`，`sourceType` 如实记为**健康科普**；只支撑风池作条件加穴 |
| GB/T 针刺操作规范 | 操作边界（进针、补泻、留针、禁忌） | `regulatory_primary` |

## 三、技术落库（按裁定的五条要求）

1. **不动全局 `upper_airway`/`respiratory` 优先级**——那会影响所有病例。
   改为新增前置闸门 `preciseSyndromeGate`，只对声明了它的模板生效。
2. **两把钥匙同时对上**：`requireCurrentFactAny`（**当前**咳嗽事实）
   + `requireSignedSyndromeAny`（**已签名**风寒袭肺/风寒束肺）。
   「当前事实」取 `treatmentCurrentFacts`（主诉/现病史/四诊，已阳性化，**不含既往史**），
   因此「否认咳嗽」与「仅有既往咳嗽」都不命中。
3. **排除范围逐条落库**：流感、流行性感冒、恢复期、风热、肺脾气虚、肺胃阴虚、气阴两伤。
   任一命中即出局，方向取保守侧。
4. **「流清涕」只决定加不加风池**，不改变整例走哪条模板。
5. **频次不照搬流感专项方案**的「每日1次、每次30分钟」：
   改为「门诊项目频次参考按本机构针刺排程执行；单次留针时长与疗程由面诊医师按本例耐受和复评结果确定」。

「仅限针刺项目内部」这条边界**钉在构建期**而不是写在代码里（写在代码里会漂）：
别的项目哪天声明了闸门或条件加穴，`npm run build:tcm-governance-tables` 直接失败。

### 实现中被自己的用例抓到的一个真缺陷

闸门最初裸用 `includes`，于是「**无**咳嗽」里的「咳嗽」照样命中——正是本仓库显式测过的
「把否认当阳性」那一类。已加极性判据：只看紧邻词前、被标点截断的一小段，
「恶寒无汗，咳嗽」里的「无」属上一短句，不能算到「咳嗽」头上（反向误判更难发现）。
排除侧刻意保持裸 `includes`——排除是保守方向。

## 四、签字前的当前行为

- 模板**整条不启用**，`protocolStatus` 仍是 `assessment_only_no_patient_specific_protocol`。
- 但**不静默**：新增出参 `deferredGovernedTemplate` 如实告知
  「已匹配到哪条待签字模板、它的取穴是什么、为什么本轮不用」，
  服务端 Markdown / 医生页面 / HIS **三个出口都下发**。
- 评估态的候选穴位**次序**也已受益（不新增任何穴）：
  用已命中双钥匙闸门的待签字模板给本来就够格的穴排先后，实测

  | | 前 5 |
  |---|---|
  | 重排前 | 合谷、风门、**承灵**、**孔最**、**肩中俞** |
  | 重排后 | 合谷、风门、太渊、**列缺**、肺俞 |

  甲方点名的三个穴已退出前 5。这不构成「启用未签字模板」：入选判据仍是
  「教材主治命中本例已记录症状」，没有新增穴、没有频次疗程、状态仍是评估态。

## 五、签字怎么生效

1. 编辑 `src/data/tcm-acupoint-syndrome-refinement-adjudications.source.json`，
   把 `common-cough-wind-cold-template` 的 `adjudicationStatus` 改为 `"approved"`，
   并在 `basis` 里追加签字人与日期。
2. `npm run verify:release`（typecheck + lint + 118 套确定性回归 + build）。
3. `IMAGE_TAG=<新tag> ./scripts/deploy-prod.sh` + `npm run verify:deployed-image`。

生效后该项目 `protocolStatus` 从评估态升为 `governed_class_template_not_syndrome_tailored`
（病种模板态），`deferredGovernedTemplate` 不再下发。对外接口文档 V1.8 已就此提前告知集成方。

## 六、回归覆盖

`npm run test:common-cough-template` —— 裁定点名的 7 条逐条钉住：

| # | 情形 | 期望 |
|---|---|---|
| 1 | 风寒咳嗽 + 鼻塞流清涕 | 含列缺、风门、合谷、**风池** |
| 2 | 风寒咳嗽，无鼻窍/头项症状 | 含列缺、风门、合谷，**不加**风池 |
| 3 | 单纯鼻炎无咳嗽 | 不得命中 |
| 4 | 流感 | 只命中流感专项模板 |
| 5 | 感染恢复期 | 只命中恢复期模板 |
| 6 | 风热咳嗽 | 不得命中风寒模板 |
| 7 | 否认咳嗽 / 仅有既往咳嗽 | 不得命中 |

另加：闸门仅限针刺项目、频次不得照搬流感方案、未签字则不启用但不静默、
逐穴溯源里风池单列 `conditional_point` 且不与主穴同源同级、重排不新增穴。

> 红旗病例由更上游的门禁拦截（`compileTcmTreatmentRecommendations` 在
> `safetyGate.status === "red_flag"` 时整条不返回治疗建议），不依赖本模板的排除项。

## 七、仍缺的一块

**风热咳嗽**目前没有对应的受治理取穴模板。需要的话按同一流程再出一份裁定稿。
